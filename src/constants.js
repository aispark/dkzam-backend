const EXPORT_ORDER_PATH = `${__dirname}/order.xlsx`.replace(/\\/g, "/");
const EXPORT_INVOICE_PATH = `${__dirname}/invoice.xlsx`.replace(/\\/g, "/");

export { EXPORT_ORDER_PATH, EXPORT_INVOICE_PATH };
