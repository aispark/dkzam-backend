const EXPORT_ORDER_PATH = `${__dirname}/order.xlsx`.replace(/\\/g, "/");
const EXPORT_INVOICE_PATH = `${__dirname}/invoice.xlsx`.replace(/\\/g, "/");
const EXPORT_DISPLAY_INVOICE_PATH = `${__dirname}/display_invoice.xlsx`.replace(
  /\\/g,
  "/"
);

export { EXPORT_ORDER_PATH, EXPORT_INVOICE_PATH, EXPORT_DISPLAY_INVOICE_PATH };
